% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/id89_figlabels.R
\name{id89_figlabels}
\alias{id89_figlabels}
\title{Derive ID89 figure-axis labels from canonical IndelType strings}
\usage{
id89_figlabels(indel_types)
}
\arguments{
\item{indel_types}{Character vector of ID89 IndelType strings, typically
\code{catalog_row_order()$ID89}.}
}
\value{
A character vector of the same length as \code{indel_types}, giving the
figure-label form of each channel name.
}
\description{
Rewrites the canonical ID89 channel names from \code{catalog_row_order()$ID89}
into the compact tick-label style used on the x-axis of \code{plot_ID89()}.
}
\details{
Single-base channels (matching \code{Del(C)}, \code{Del(T)}, \code{Ins(C)}, \code{Ins(T)}):
the \code{Del(..)}/\code{Ins(..)} wrapper and the \verb{:R} prefix are dropped, and any
open-ended repeat range \verb{(N,)} is rewritten as \verb{(N+)}.

Multi-base channels (longer Del/Ins/MH rows): leading \verb{Del(}/\verb{Ins(} is
rewritten to \verb{L(}, a leading open-ended length \verb{(N,)} is space-padded to
\verb{(N, )}, a trailing \code{R(N,)} becomes \verb{R(N+)}, a trailing \code{M(N,)} is
space-padded to \code{M(N, )}, and all colons are removed.

The \code{"Complex"} channel is returned unchanged.
}
\examples{
id89_figlabels(c(
  "[Del(C):R1]A",
  "Del(C):R(6,9)",
  "Del(2,4):R1",
  "Del(2,):U(1,2):R(5,)",
  "Del(6,):M(4,)",
  "Complex"
))

}
