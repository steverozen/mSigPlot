% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_guess.R, R/plot_guess_pdf.R
\name{plot_guess}
\alias{plot_guess}
\alias{plot_guess_pdf}
\title{plot_guess, plot_guess_pdf}
\usage{
plot_guess(catalog, ...)

plot_guess_pdf(catalog, filename, ...)
}
\arguments{
\item{catalog}{Numeric vector, single-column data.frame, matrix, tibble,
or data.table. The number of rows (or length) determines which plotting
function is used:
\itemize{
\item 1536 rows: calls \code{plot_SBS1536()}
\item 476 rows: calls \code{plot_ID476()}
\item 288 rows: calls \code{plot_SBS288()}
\item 192 rows: calls \code{plot_SBS192()}
\item 166 rows: calls \code{plot_ID166()}
\item 144 rows: calls \code{plot_DBS144()}
\item 136 rows: calls \code{plot_DBS136()}
\item 96 rows: calls \code{plot_SBS96()}
\item 89 rows: calls \code{plot_ID89()}
\item 83 rows: calls \code{plot_ID83()}
\item 78 rows: calls \code{plot_DBS78()}
}

The column names of \code{catalog} are used as plot titles.}

\item{...}{Additional arguments passed to the underlying plotting function.}

\item{filename}{Character. Path to the output PDF file (\code{plot_guess_pdf} only).}
}
\value{
\code{plot_guess()} returns a plot object. The exact class depends on
the dispatched function: most return a ggplot object; SBS288, DBS136,
and SBS1536 return patchwork objects. Note: adding ggplot2 layers with
\code{+} to a patchwork object (e.g. \code{+ ggtitle()}) applies only to the last
sub-plot, not the composite; use \code{\link[patchwork:plot_annotation]{patchwork::plot_annotation()}} instead.
\code{plot_guess_pdf()} returns NULL invisibly (called for side effect of
creating a PDF file).
}
\description{
Automatically select the appropriate plotting function based on the number
of rows in the catalog.
}
\details{
\code{plot_guess()} plots a single sample. \code{plot_guess_pdf()} creates a
multi-page PDF file containing plots for multiple samples, arranged with
5 samples per page (except heatmap types: 1 per page). Uses Cairo for
high-quality PDF rendering.
}
\examples{
# Auto-detect a 96-channel catalog and dispatch to plot_SBS96
set.seed(1)
sig <- runif(96)
sig <- sig / sum(sig)
names(sig) <- catalog_row_order()$SBS96
plot_guess(sig, plot_title = "Auto-detected SBS96")

\dontrun{
sig <- matrix(runif(96 * 3), nrow = 96)
rownames(sig) <- catalog_row_order()$SBS96
colnames(sig) <- paste0("Sig", 1:3)
plot_guess_pdf(sig, filename = "auto_detected.pdf")
}

}
\seealso{
\code{\link[=plot_SBS96]{plot_SBS96()}}, \code{\link[=plot_SBS192]{plot_SBS192()}}, \code{\link[=plot_SBS288]{plot_SBS288()}}, \code{\link[=plot_SBS1536]{plot_SBS1536()}},
\code{\link[=plot_DBS78]{plot_DBS78()}}, \code{\link[=plot_DBS136]{plot_DBS136()}}, \code{\link[=plot_DBS144]{plot_DBS144()}}, \code{\link[=plot_ID166]{plot_ID166()}},
\code{\link[=plot_ID476]{plot_ID476()}}, \code{\link[=plot_ID89]{plot_ID89()}}, \code{\link[=plot_ID83]{plot_ID83()}}
}
