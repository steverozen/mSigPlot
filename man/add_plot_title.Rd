% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{add_plot_title}
\alias{add_plot_title}
\title{Add a plot title, inside the panel (annotate) or above it (ggtitle)}
\usage{
add_plot_title(
  p,
  plot_title,
  title_outside_plot,
  plot_title_cex,
  base_size,
  ymax,
  x = 1,
  y_frac = 7.4/8,
  hjust = 0
)
}
\arguments{
\item{p}{A ggplot object.}

\item{plot_title}{Character title (NULL or empty = no title added).}

\item{title_outside_plot}{Logical. FALSE = inside, TRUE = above.}

\item{plot_title_cex}{Numeric size multiplier.}

\item{base_size}{Numeric base font size in points.}

\item{ymax}{Numeric. Top of the plotting region used to position the
inside title. Ignored when \code{title_outside_plot = TRUE}.}

\item{x}{Numeric x-coordinate for the inside title.}

\item{y_frac}{Numeric. Inside title sits at \code{ymax * y_frac}.}

\item{hjust}{Horizontal justification of the inside title.}
}
\value{
The ggplot object with the title added.
}
\description{
If \code{title_outside_plot} is FALSE, places \code{plot_title} inside
the plot area via \code{annotate("text", ...)} at \code{y = ymax * y_frac} — the
\code{plot_ID83} style. If TRUE, places it above the plot via
\code{ggtitle() + theme(plot.title = ...)}. Title size is always
\code{plot_title_cex * base_size} (points), normalized to mm for \code{annotate}.
}
\keyword{internal}
