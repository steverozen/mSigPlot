% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_SBS288.R
\name{plot_SBS288}
\alias{plot_SBS288}
\title{Plot an SBS288 catalog as a 3-panel stacked SBS96 plot}
\usage{
plot_SBS288(catalog, plot_title = NULL, ...)
}
\arguments{
\item{catalog}{Numeric vector, single-column data.frame, matrix, tibble,
or data.table with 288 rows. Row names must have 2-character strand
prefixes (\verb{T:}, \verb{U:}, \verb{N:}).}

\item{plot_title}{Character. Optional overall title for the combined plot.}

\item{...}{Additional arguments passed to \code{\link[=plot_SBS96]{plot_SBS96()}}.}
}
\value{
A \code{patchwork} object (printable and compatible with \code{ggsave()}).
}
\description{
Takes a 288-row catalog (3 strand categories x 96 SBS channels) and produces
a vertically stacked 3-panel SBS96 plot with a shared y-axis maximum.
Row name format: \verb{T:A[C>A]T}, \verb{U:A[C>A]T}, \verb{N:A[C>A]T} where \verb{T:} is
template (transcribed), \verb{U:} is non-template (untranscribed), and \verb{N:} is
not-transcribed (intergenic).
}
\examples{
set.seed(1)
sbs96_names <- catalog_row_order()$SBS96
rnames <- c(paste0("T:", sbs96_names),
            paste0("U:", sbs96_names),
            paste0("N:", sbs96_names))
sig <- runif(288)
sig <- sig / sum(sig)
names(sig) <- rnames
plot_SBS288(sig, plot_title = "Example SBS288")

}
