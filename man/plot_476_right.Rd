% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_476_right.R
\name{plot_476_right}
\alias{plot_476_right}
\title{Plot right portion of 476-channel indel profile (positions 343-476)}
\usage{
plot_476_right(
  catalog,
  block_text_size = 0.78,
  plot_title = "test",
  num_labels = 3,
  ggrepel_size = 0.52,
  label_threshold_denominator = 7,
  vline_labels = c(),
  simplify_labels = TRUE,
  base_size = 11,
  title_text_size = 1,
  x_axis_tick_label_size = 0.8,
  y_axis_tick_label_size = 0.7,
  x_title_size = 0.7,
  y_title_size = 0.9,
  plot_complex = FALSE,
  text_size = NULL,
  label_size = NULL,
  show_counts = NULL,
  count_label_size = 1.03
)
}
\arguments{
\item{catalog}{Numeric vector of length 476 containing indel counts or
proportions for a single sample. Only positions 343-476 are plotted.}

\item{block_text_size}{Numeric. Size of category block labels (e.g. "Del >=2bp"),
as a fraction of \code{base_size}.}

\item{plot_title}{Character. Title displayed above the plot.}

\item{num_labels}{Integer. Number of top peaks to label per category block.
Set to 0 or NULL to disable labels.}

\item{ggrepel_size}{Numeric. Size of ggrepel peak labels, as a fraction of
\code{base_size}.}

\item{label_threshold_denominator}{Numeric. Peaks with values less than
max/label_threshold_denominator are not labeled.}

\item{vline_labels}{Character vector. IndelType labels at which to draw
vertical reference lines. Positions are adjusted to the 1-134 coordinate
space.}

\item{simplify_labels}{Logical. If TRUE, simplifies peak labels by removing
the indel type prefix.}

\item{base_size}{Base font size for ggplot2's theme. All text sizes scale
relative to this value.}

\item{title_text_size}{Numeric. Relative size of the plot title text, passed to \code{rel()}.}

\item{x_axis_tick_label_size}{Numeric. Relative size of x-axis tick labels, passed to \code{rel()}.}

\item{y_axis_tick_label_size}{Numeric. Relative size of y-axis tick labels, passed to \code{rel()}.}

\item{x_title_size}{Numeric. Relative size of x-axis title, passed to \code{rel()}.}

\item{y_title_size}{Numeric. Relative size of y-axis title, passed to \code{rel()}.}

\item{plot_complex}{Logical. If TRUE, include the 5 Complex indel channels.}

\item{text_size}{Deprecated. Use \code{block_text_size} instead.}

\item{label_size}{Deprecated. Use \code{ggrepel_size} instead.}

\item{show_counts}{Logical or NULL. If \code{TRUE}, always display per-class
mutation count labels. If \code{FALSE}, never display them. If \code{NULL}
(the default), display them only when the catalog contains counts
(sum > 1.1).}

\item{count_label_size}{Numeric. Size of per-class count labels, as a
fraction of \code{base_size}.}
}
\value{
A ggplot2 object containing the right portion of the 476-channel
indel profile plot.
}
\description{
Creates a bar plot visualization of the right 134 channels of a 476-channel
indel mutational signature. This covers the last 4 category blocks:
Del(2,):R(1,9), Ins(2,):R(0,9), Del(2,):M(1,), and Complex.
}
